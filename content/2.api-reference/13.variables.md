---
title: Параметры
description: ""
---

## Создать параметр

::field-group
  :::field{name="HTTP"}
  POST `/variables`:br Создаёт новый параметр.
  :::

  :::field{required name="Авторизация" type="string"}
  bearerAuth `JWT-токен`
  :::

  :::field{name="Тело запроса (application/json):"}
  - `id` (string · uuid) — уникальный идентификатор параметра
  - `name` (string) — имя параметра (напр.: `API_KEY`)
  - `value` (string | nullable) — значение параметра
  - `type` (string) — тип переметра (напр.: `string`, `number`)
  - `createdDate` (string · date-time) — дата/время создания
  - `updatedDate` (string · date-time) — дата/время обновления
  :::
::

Коды ответов:

- 200 — параметр успешно создан (application/json)
- 400 — некорректное тело запроса
- 422 — ошибка валидации
- 500 — внутренняя ошибка сервера

```HTTP
POST /variables HTTP/1.1
Authorization: Bearer JWT
Content-Type: application/json
```

```json [Пример запроса]
{
  "id": "cfd531e0-82fc-11e9-bc42-526af7764f64",
  "name": "API_KEY",
  "value": "my-secret-key",
  "type": "string",
  "createdDate": "2024-08-24T14:15:22Z",
  "updatedDate": "2024-08-24T14:15:22Z"
}
```

```json [Пример ответа (200)]
{
  "id": "cfd531e0-82fc-11e9-bc42-526af7764f64",
  "name": "API_KEY",
  "value": "my-secret-key",
  "type": "string",
  "createdDate": "2024-08-24T14:15:22Z",
  "updatedDate": "2024-08-24T14:15:22Z"
}
```

## Список параметров

::field-group
  :::field{name="HTTP"}
  GET `/variables`
  Возвращает список всех параметров.
  :::

  :::field{required name="Авторизация" type="string"}
  bearerAuth `JWT-токен`
  :::
::

Коды ответов:

- 200 — успешная операция (application/json, массив объектов)
- 500 — внутренняя ошибка сервера

```json [Пример ответа]
[
  {
    "id": "cfd531e0-82fc-11e9-bc42-526af7764f64",
    "name": "API_KEY",
    "value": "my-secret-key",
    "type": "string",
    "createdDate": "2024-08-24T14:15:22Z",
    "updatedDate": "2024-08-24T14:15:22Z"
  }
]
```

## Обновить переметр по ID

::field-group
  :::field{name="HTTP"}
  PUT `/variables/{id}`
  Обновляет указанный параметр.
  :::

  :::field{required name="Авторизация" type="string"}
  bearerAuth `JWT-токен`
  :::

  :::field{name="Параметры пути"}
  `id` (string) — Variable ID
  :::

  :::field{name="Тело запроса (application/json):"}
  Те же поля, что и при создании.
  :::
::

Коды ответов:

- 200 — переменная успешно обновлена (application/json)
- 400 — некорректный ID или тело запроса
- 404 — переменная не найдена
- 500 — внутренняя ошибка сервера

```HTTP
PUT /variables/{id} HTTP/1.1
Authorization: Bearer JWT
Content-Type: application/json
```

```json [Пример запроса]
{
  "id": "cfd531e0-82fc-11e9-bc42-526af7764f64",
  "name": "API_KEY",
  "value": "my-secret-key",
  "type": "string",
  "createdDate": "2024-08-24T14:15:22Z",
  "updatedDate": "2024-08-24T14:15:22Z"
}
```

```json [Пример ответа (200):]
{
  "id": "cfd531e0-82fc-11e9-bc42-526af7764f64",
  "name": "API_KEY",
  "value": "my-secret-key",
  "type": "string",
  "createdDate": "2024-08-24T14:15:22Z",
  "updatedDate": "2024-08-24T14:15:22Z"
}
```

## Удалить переменную по ID

::field-group
  :::field{name="HTTP"}
  DELETE `/variables/{id}`:br Удаляет указанный параметр.
  :::

  :::field{required name="Авторизация" type="string"}
  bearerAuth `JWT-токен`
  :::

  :::field{required name="Параметры пути" type="string"}
  `id` (string) — Variable ID
  :::
::

Коды ответов:

- 200 — параметр удалена (без содержимого)
- 400 — некорректный ID
- 404 — параметр не найден
- 500 — внутренняя ошибка сервера
